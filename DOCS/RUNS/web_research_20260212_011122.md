# Web Research

Query: openclaw gateway security trusted proxies
Generated: 2026-02-12T01:11:30-03:00

No pude usar `web_search` porque falta la clave de API de Brave (error `missing_brave_api_key`). Podés guardarla con `openclaw configure --section web` o definiendo `BRAVE_API_KEY` en el entorno del Gateway, y luego lo intento de nuevo. ¿Querés que siga otro camino mientras tanto?
